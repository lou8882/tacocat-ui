import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: '#e3f2fd',
    borderRadius: 8,
    padding: 10,
    marginBottom: 15,
  },
  speedText: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#1976d2',
  },
  label: {
    fontSize: 12,
    color: '#666',
    marginTop: 2,
  },
});